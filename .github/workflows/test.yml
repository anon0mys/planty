
name: Test

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:14-alpine
        ports:
          - "5432:5432"
        env:
          POSTGRES_DB: planty_test
          POSTGRES_USER: planty
          POSTGRES_PASSWORD: ""
    env:
      RAILS_ENV: test
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Build
        uses: actions/setup-ruby@359bebbc29cbe6c87da6bc9ea3bc930432750108
        with:
          bundler-cache: true
          ruby-version: '2.7.2'
      - name: Setup DB
        env:
          RAILS_ENV: test
          PGHOST: localhost
          PGUSER: planty
        run: bin/rails db:setup
      - name: Test
        run: bin/rspec
